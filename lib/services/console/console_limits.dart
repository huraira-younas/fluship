class ConsoleLimits {
  const ConsoleLimits._();

  static const maxLineLength = 256 * 1024;
  static const maxLinesPerSession = 2000;
  static const maxCommandHistory = 50;
  static const flushIntervalMs = 48;
  static const maxSessions = 3;
}
